# protogenOS packages

Future PKGBUILDs belong here. The expected initial packages are:

- `protogenos-release` — operating-system identity and release metadata
- `protogenos-branding` — logo, wallpapers, and boot artwork
- `protogenos-settings` — desktop defaults and system configuration

Branding assets must record their author, source, and redistribution license.
